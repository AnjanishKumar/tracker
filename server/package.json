{
  "name": "server",
  "version": "1.0.0",
  "description": "A node js server application for tracker using nodejs and express",
  "main": "index.js",
  "scripts": {
    "dev": "./node_modules/nodemon/bin/nodemon.js src/app.js --exec 'npm run lint && node'",
    "start": "npm run dev",
    "build": "echo \"Error: no build step specified\" && exit 1",
    "unit": "echo \"Error: no unit test specified\" && exit 1",
    "e2e": "echo \"Error: no e2e test spefified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --ext .js src test/unit/specs test/e2e/specs"
  },
  "author": "Anjanish Kumar <anjanishvikas@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^4.10.0",
    "eslint-config-google": "^0.9.1",
    "nodemon": "^1.12.1"
  },
  "dependencies": {
    "body-parser": "^1.18.2",
    "cors": "^2.8.4",
    "express": "^4.16.2",
    "morgan": "^1.9.0",
    "winston": "^2.4.0"
  }
}
